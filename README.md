# kita-records-site

限定公開用の閲覧ページ。中身はAES-256-GCMで暗号化されており、**完全なリンク（URLフラグメントの鍵つき）を知っている人だけ**が閲覧できます。

- このリポジトリに平文コンテンツは含まれません（暗号化ブロブのみ）
- 鍵はURLの `#` 以降で渡されるため、サーバーやアクセスログには残りません
- ソースの更新・再ビルドは非公開リポジトリ側（kita-records）の `build-gate.mjs` で行います
